print('Hi Git')

# Use git init at the root of the project.
# Git file will have a "." in front of it, to see it, turn on the hidden files.
# Use git status to see untracked files. red are untracked, green are tracked but not commited.
# Use git add . to add files to the repository.
# Use git commit -m "enter description here" to commit the files and folders to Git.
# Use git push to advance changes up to gitHub for version control.
# Use .gitignore file to list the names of files and folders to ignore during the push. 
# Add a / after folder names to include all files within the folder for ignore.
# Use git pull to bring changes made in gitHub down to your local machine.



print('Hi from another dev')
